# AI 会议项目协作说明

## 文件用途

本文件是项目的长期背景与协作约定，供参与本项目的 AI 在每次开始工作前阅读。它用于在不同模型、不同会话和不同上下文窗口之间保持一致的项目认知。

AI 开始任务时，应先阅读本文件，再结合仓库中的代码、文档和当前任务开展工作。不得只依据本文件猜测当前实现状态，必要时应检查实际代码和版本记录。

本文件不是一次性说明。随着产品方向、技术方案、重要决策和工作流程发生变化，AI 应同步更新本文件，确保它持续反映项目的真实状态。

## 项目愿景

市面上的人工智能模型各有优缺点，可以把它们理解为从不同地方招聘来的员工。本项目希望把多个 AI 邀请到同一个“会议室”，围绕用户提出的问题独立思考、讨论、质询和修订观点，最终帮助用户获得更可靠、更全面且可追溯的决策结果。

项目的目标不是简单地让多个模型同时回答同一道题，也不是展示热闹的聊天过程，而是建立一套有效的多智能体决策机制，减少单一模型的盲点，并让共识、分歧、风险和不确定性清晰可见。

## 核心原则

1. **独立思考**：各模型应先独立作答，在形成初步判断前不查看其他模型的意见，降低锚定和从众影响。
2. **角色分工**：根据议题为参与者分配明确职责，例如方案提出者、反方辩手、风险审查员、事实核查员、用户利益代表和会议主持人。
3. **交叉质询**：模型需要检查其他方案的前提、证据、逻辑缺口和失败风险，不能只做重复回答或无依据地表示赞同。
4. **允许修订**：质询结束后，各方可以更新观点，但应说明改变了什么以及改变的原因。
5. **透明决议**：最终结果不能只有投票或单一结论，还应呈现依据、反对意见、不确定性、置信程度，以及哪些新信息可能改变结论。
6. **人类拥有最终决定权**：AI 会议提供分析和建议，不应把多模型共识伪装成绝对正确的答案。
7. **质量优先于数量**：清晰的议事规则和互补的角色通常比无组织地接入大量模型更有价值。

## 初步产品流程

第一阶段可采用以下最小可行流程：

1. 用户提交一个待讨论的问题，并补充目标、限制条件和必要背景。
2. 3 至 5 个模型分别独立分析并提交初始意见。
3. 各模型互相审阅，指出关键假设、冲突、证据缺口与潜在风险。
4. 模型根据质询修订自己的观点。
5. 主持模型整理共识和分歧，形成结构化会议纪要与行动建议。
6. 用户查看完整依据并作出最终选择，必要时发起下一轮讨论。

## 预期输出

一次完整会议的结果应尽量包含：

- 推荐方案及其适用条件
- 支持该方案的主要理由
- 核心共识与关键分歧
- 主要反对意见和风险清单
- 尚未解决的问题与信息缺口
- 各参与模型的立场、置信度及立场变化
- 可能改变当前结论的新信息
- 可执行的下一步行动

产品界面可以采用会议室的交互形式，但不应只展示冗长的聊天记录。结果页应优先帮助用户快速理解结论、依据、分歧和风险。

## 当前阶段

项目已建立可交互的“人类参与式 AI 会议”原型，用于验证“创建议题—AI 独立开场—用户主持发言—AI 针对回应—用户决定何时整理纪要”的核心体验。原型采用 React 与 Vite，通过仅限本地开发的服务端代理接入 OpenAI 兼容模型 API；尚未实现生产后端、账户系统或持久化存储。

初版将产品范围收敛为：

- 用户填写会议议题、决策目标、必要背景和限制条件。
- 用户从方案提出者、反方辩手、风险审查员、事实核查员和用户利益代表中选择 3 至 5 位 AI 参与者，用户本人担任会议主持人。
- AI 先独立提交开场观点和问题，随后会议必须停下来等待用户补充事实、提出质疑、表达偏好或要求某个方向继续展开。
- 用户每次发言后，各 AI 议事席针对完整讨论记录作出回应。单个模型失败不得阻断整场会议，应在对应席位透明显示失败状态。
- 会议进行中，用户可以为单个 AI 议事席切换模型；切换从下一轮发言生效，历史发言继续标记其实际使用的模型。
- 只有用户主动点击“形成阶段纪要”后，主持模型才整理当前方案、依据、分歧、风险和下一轮待决问题。阶段纪要不是替用户作出的最终决定。

下一阶段应先验证用户是否真正愿意参与多轮讨论，以及 AI 是否能针对用户发言产生有价值的观点变化，再确定模型编排、后端存储、鉴权和部署架构。当前 Vite 代理仅供本地开发，不能作为生产安全边界。

## AI 工作约束

AI 在参与本项目时应遵守以下要求：

1. 开始工作前阅读本文件，并检查与任务相关的代码、文档和版本状态。
2. 先理解现有实现和已有决策，再提出或实施修改，避免凭空假设。
3. 修改应围绕当前任务，保持范围清晰，不随意引入无关功能或重构。
4. 新决策若影响项目愿景、产品范围、架构、关键流程、数据约定或协作方式，应在同一次任务中更新本文件或对应的专题文档。
5. 若新信息推翻旧结论，应直接修订过时内容，并在适当位置记录变更原因，避免保留互相冲突的说明。
6. 不得把尚未验证的想法描述为已经实现的功能；“计划”“已决定”和“已完成”必须明确区分。
7. 对高风险决策、事实性结论和外部信息，应保留依据，并明确不确定性。
8. 除非用户明确要求，AI 不应替用户作出不可逆的业务决定。

## 每次任务完成后的要求

AI 完成一次任务后，应进行以下检查：

1. 验证改动是否满足当前任务，并尽可能运行相关测试、构建或检查命令。
2. 检查是否引入与任务无关的修改，不得擅自覆盖用户或其他协作者的改动。
3. 判断本次工作是否产生了需要长期保留的新背景、产品决策或协作约定；如有，立即同步更新本文件或相关文档。
4. 清楚说明完成了什么、如何验证、仍有哪些限制或未解决问题。
5. 检查版本管理状态和改动差异；有必要时进行版本管理，形成范围明确、说明清晰的提交。执行提交、推送等操作时，应遵守当前环境的权限要求和用户指令，不得擅自覆盖或丢弃已有历史。

## 文件维护规则

- 本文件只保留跨会话仍有价值的信息，不记录临时调试过程或容易失效的细节。
- 内容应简洁、明确、可执行，避免把同一规则分散重复书写。
- 当内容逐渐复杂时，可以拆分为产品、架构、决策记录等专题文档，并在本文件中提供索引。
- AI 对项目做出重要修改时，应把文档同步视为任务的一部分，而不是可选的后续工作。
- 若本文件与用户在当前会话中的最新明确指令冲突，以用户的最新指令为准，并在需要时更新本文件。
